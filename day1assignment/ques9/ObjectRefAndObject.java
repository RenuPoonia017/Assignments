package com.day1assignmentques.ques9;
import java.util.*;
import java.util.ArrayList;


public class ObjectRefAndObject {

	public static void main(String[] args) {
		ObjectRefAndObject objRef=new ObjectRefAndObject();
		
		System.out.println(objRef);
		
		/*
		 * 
		 * Object is an actual implementation of a class. 
		 * When we create an object, it is stored in a heap memory. 
		 * and the address of the object in the memory will be stored in the 
		 * object reference variable. If we print the object reference it will 
		 * print the memory address in hex-decimal format using the toString() method 
		 * available in the Object class.
		 */
	}
}
